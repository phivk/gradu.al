{
  "name": "gradual-landing",
  "version": "1.0.0",
  "description": "Imagine what your peers are good at... and then learning it, gradually. Together.",
  "author": "Philo van Kemenade",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt generate",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "nuxt generate && push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxt/content": "^1.11.1",
    "lodash": "^4.17.20",
    "nuxt": "^2.14.7",
    "tachyons": "^4.12.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
